<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
</head>
<body>
    <a href="/">Home</a>
    <% if(!user) { %>
        <a href="/auth/login">Login</a>
        <%} else {%>
        <a href="/auth/logout">Logout</a>
    <% } %>
    <h1>Profile Page</h1>
    <h2>You are logged in, Your username is - <%=user.userName%></h2>
    <br>
    <img src=<%=user.thumbnail%> alt="no_preview">
    <p>This is your profile picture.</p>
</body>
</html>